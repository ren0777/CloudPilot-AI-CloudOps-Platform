import { index, integer, real, sqliteTable, text } from 'drizzle-orm/sqlite-core';
export const resources = sqliteTable('resources', {id:text('id').primaryKey(),name:text('name').notNull(),kind:text('kind').notNull(),region:text('region').notNull(),cpu:real('cpu').notNull(),memory:real('memory').notNull(),latency:real('latency').notNull(),cost:real('cost').notNull(),source:text('source').notNull(),updated:text('updated').notNull()});
export const incidents = sqliteTable('incidents',{id:text('id').primaryKey(),resourceId:text('resource_id').notNull(),title:text('title').notNull(),severity:text('severity').notNull(),status:text('status').notNull(),evidence:text('evidence').notNull(),created:text('created').notNull(),resolved:text('resolved')}, t=>[index('idx_incidents_resource_status').on(t.resourceId,t.status)]);
export const audit = sqliteTable('audit',{id:text('id').primaryKey(),action:text('action').notNull(),detail:text('detail').notNull(),created:text('created').notNull()});
export const samples = sqliteTable('samples',{id:integer('id').primaryKey({autoIncrement:true}),resourceId:text('resource_id').notNull(),cpu:real('cpu').notNull(),memory:real('memory').notNull(),latency:real('latency').notNull(),created:text('created').notNull()});
